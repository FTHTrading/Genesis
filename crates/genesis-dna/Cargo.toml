[package]
name = "genesis-dna"
version.workspace = true
edition.workspace = true

[features]
# Enable the deterministic feature to assert at compile-time that no
# wall-clock time or OS-entropy calls can enter experiment paths.
# All production experiment code should be built with this feature enabled.
deterministic = []

[dependencies]
serde.workspace = true
serde_json.workspace = true
sha2.workspace = true
rand.workspace = true
rand_chacha.workspace = true
chrono.workspace = true
uuid.workspace = true
thiserror.workspace = true
hex.workspace = true
